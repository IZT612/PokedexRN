# Theme Tokens

Shared design tokens for the Pokédex app.

- `brandColors` for base UI colors
- `pokemonTypeColors` for semantic type styling
- `typography` for font family, sizes, weights, and line heights
- `spacing` for layout rhythm
- `tokens` as the stable combined export
