## 1. Token Foundation

- [x] 1.1 Create a shared token module for brand colors, semantic text colors, typography, and spacing.
- [x] 1.2 Add Pokémon type color tokens for every type defined in the PRD.
- [x] 1.3 Export tokens through a stable interface that can be imported by UI components.

## 2. UI Integration

- [ ] 2.1 Update existing shared UI styles to consume the new tokens instead of hard-coded values.
- [ ] 2.2 Wire type badges, borders, and filter UI to the semantic type token map.
- [ ] 2.3 Replace ad-hoc spacing and font values in reused components with token-based values.

## 3. Verification

- [x] 3.1 Validate that the token values match the PRD palette and typography guidance.
- [ ] 3.2 Confirm the updated UI renders consistently on mobile and web.
- [ ] 3.3 Check for any components still using hard-coded color, font, or spacing values.
